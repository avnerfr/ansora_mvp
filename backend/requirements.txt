fastapi==0.104.1
# Use uvicorn without [standard] extras to save space (uvloop, httptools are optional)
uvicorn==0.24.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6
pydantic
email-validator>=2.0.0  # Required for EmailStr validation (lightweight, ~1-2MB)
sqlalchemy==2.0.23
aiosqlite==0.19.0
# LangChain - minimal install (only what we need)
langchain-core==0.2.38
langchain-community==0.2.12
langchain-openai==0.1.23  # Only needed for ChatOpenAI (LLM), not embeddings
qdrant-client>=1.11.0,<2.0.0
openai>=1.40.0,<2.0.0  # Only needed for ChatOpenAI (LLM), not embeddings
sentence-transformers>=2.2.0
# Removed: langchain==0.2.16 (meta-package, not needed - we use specific packages)
pypdf==3.17.4
python-pptx==0.6.23
# Optional: OCR support (can be removed if not needed)
# pillow==10.1.0
# pytesseract==0.3.10
python-dotenv==1.0.0
nltk==3.8.1
httpx==0.27.0
boto3>=1.34.0
google-api-python-client>=2.100.0
google-auth-httplib2>=0.1.1
google-auth-oauthlib>=1.1.0
# Removed: unstructured[ppt]>=0.11.0 (very heavy, ~1-2GB, replaced with python-pptx)

